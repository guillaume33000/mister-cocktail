<h1>Let's create a new cocktail :</h1>

<%= simple_form_for(@cocktail) do |f| %>
  <%= f.error_notification %>
  <%= f.input :name %>
  <%= f.input :picture, as: :url %>
  <%#= f.input :ingredient, collection: @ingredients %>
  <%= f.button :submit, class: "btn btn-warning" %>
<% end %>
